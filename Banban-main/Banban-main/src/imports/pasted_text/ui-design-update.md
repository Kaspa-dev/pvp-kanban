Improve the overall layout, navigation bar design, and visual quality of the application.

1. Fix navigation bar design (top bar)

The current top navigation bar looks too flat and empty.

Improve it:

Add subtle accent color integration

Use a soft gradient or tinted background based on the current accent color

Add slightly stronger shadow or elevation

Increase contrast between navbar and content

The navbar should feel like a clear UI layer, not just a plain rectangle.

2. Move user profile to the right side

Currently the user info (name, avatar, level) is on the left sidebar.

Move the main user info to the top-right corner of the navbar, next to:

settings icon

other top controls

Design it as a compact component:

[Avatar] Name (small) + level badge

Keep it clean and aligned.

You can optionally:

keep minimal info in sidebar (avatar only)

but full user info should be in the top right

3. Clean up layout structure

The layout currently looks like it is made of separate blocks with visible seams.

Fix this:

Remove visible dividing lines between layout sections

Avoid thin borders that create “boxed” feeling

Use spacing instead of borders to separate sections

Use:

padding

background contrast

shadows

instead of visible lines

4. Unify the background

Make the layout feel like one continuous surface:

sidebar and content should feel connected

avoid sharp color differences

use subtle background variations instead

5. Improve sidebar design

Sidebar is too plain.

Improve it:

add very subtle background tint

highlight active item using accent color

improve hover states

Do NOT make it too colorful — keep it clean.

6. Visual hierarchy

Make clear separation:

Navbar (top)

Sidebar (left)

Main content (board)

Each should feel like a layer, not random blocks.

7. Style reference

Follow a clean SaaS style similar to:

Linear

Notion

Jira (modern version)

Avoid:

heavy borders

visible seams

flat empty areas

8. Final goal

The UI should feel:

clean

structured

modern

professional

No visible layout bugs or awkward lines.